# 云端早报 — 还需你完成 2 步（约 3 分钟）

本地代码、Git 提交、云端工作流文件、本机定时关闭 — **已全部就绪**。  
以下两步必须在你已登录的 GitHub 里完成（我无法代替输入账号密码）。

---

## 第 1 步：推送代码（GitHub Desktop）

1. 打开 **GitHub Desktop**
2. **File → Add Local Repository** → 选择：
   ```
   /Users/yanxin/Desktop/AI_Daily_Brief
   ```
3. 点击蓝色按钮 **「Push origin」**（或 Publish branch）

✅ 成功标志：打开 https://github.com/4wvv65ddtv-cyber/AI_Daily_Brief 能看到 `ai_news_bot` 文件夹（不再是 Empty）。

---

## 第 2 步：添加 2 个 Secrets

打开（或运行 `bash scripts/open_github_setup.sh`）：

https://github.com/4wvv65ddtv-cyber/AI_Daily_Brief/settings/secrets/actions

点击 **New repository secret**，各添加一次：

| Name（名称必须一致） | Secret 值从哪复制 |
|---------------------|-------------------|
| `OPENAI_API_KEY` | 本机 `.env` 中 `OPENAI_API_KEY=` 后面整段 |
| `FEISHU_WEBHOOK_URL` | 本机 `.env` 中 `FEISHU_WEBHOOK_URL=` 后面整段 |

---

## 第 3 步（可选）：立刻试跑

https://github.com/4wvv65ddtv-cyber/AI_Daily_Brief/actions/workflows/daily-brief.yml

→ **Run workflow** → **Run workflow** → 等 1～2 分钟变绿 → 飞书应收简报。

---

## 完成后

- **每个工作日 08:00（北京时间）** 自动推送，**无需 Mac 开机/登录**
- 本机 cron / LaunchAgent 已关闭，避免重复发送

完成后回复「已推送」或「已配 Secrets」，我可帮你远程确认 Actions 是否成功。
